{
  "name": "qalsync",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter @qalsync/backend dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint"
  },
  "engines": {
    "node": ">=18"
  }
}
